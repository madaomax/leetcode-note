https://leetcode.com/problems/longest-subarray-of-1s-after-deleting-one-element/description/
medium

Given a binary array `nums`, you should delete one element from it.

Return _the size of the longest non-empty subarray containing only_ `1`_'s in the resulting array_. Return `0` if there is no such subarray.

**Example 1:**

**Input:** nums = [1,1,0,1]
**Output:** 3
**Explanation:** After deleting the number in position 2, [1,1,1] contains 3 numbers with value of 1's.

**Example 2:**

**Input:** nums = [0,1,1,1,0,1,1,0,1]
**Output:** 5
**Explanation:** After deleting the number in position 4, [0,1,1,1,1,1,0,1] longest subarray with value of 1's is [1,1,1,1,1].

**Example 3:**

**Input:** nums = [1,1,1]
**Output:** 2
**Explanation:** You must delete one element.

**Constraints:**

- `1 <= nums.length <= 105`
- `nums[i]` is either `0` or `1`.



# Solution: 可变窗口 sliding window
```python
class Solution:
    def longestSubarray(self, nums: List[int]) -> int:
        """
        Summary:
            Delete one element from `nums`
        Args:
            binary array `nums`
        Return:
            the size of the longest non-empty sub-array containing only 1's
            in the resulting array. 
            0 if there is no such subarray
        """
        max_size = 0

        # M0: probably either two pointers or sliding window
        left = 0
        right = 0
        deleted = 0
        # current subarray # of deleted elem
        # formula for size = left - r
        for right in range(len(nums)):
            # must delete one
            if nums[right] == 1:
                # no need to delete, don't update deleted
                # great. 
                pass
            else:
                # nums[right] == 0
                deleted += 1 # need to delete one more 0
                while deleted > 1:
                    # if deleted > 1, window is no longer valid.
                    # move left to the right to make window valid again
                    # i.e. deleted <= 1
                    if nums[left] == 0:
                        deleted -= 1
                    # [0, 1, 1, 1, 0, 1, 1, 0, 1]
                    left += 1
	            # = right - left + 1 - 1 (deleted)
            max_size = max(max_size, right - left)

        return max_size
```

prob already a formula. 

Time complexity: O(N) because each element in the list is going to be visited at most twice = O(2N) = O(N)

Space complexity: O(1)