# [205. Isomorphic Strings](https://leetcode.com/problems/isomorphic-strings/)

Easy

Hashtable

```python
class Solution:
    def isIsomorphic(self, s: str, t: str) -> bool:
        """
        Summary:
            Determines if s and t are isomorphic.
            Two strs `s` and `t` are isomorphic is the characters in `s` can be replaced to get `t`.
            All occurrences of a character must be replaced with another character while preserving the
            order of characters. No two characters map to the same char, but a char may map to itself.
        Args:
            s: str
            t:str

        Return:
            bool
        E.g.
            s = "egg"
            t = "add"
            True


            s = "f11"
            t = "b23"
            false
        """
        # key: s, value: t
        # for 1:1 relationship
        s_map = {}
        t_map = {}
        if len(s) != len(t):
            return False
        for i in range(len(s)):
            ss = s_map.get(s[i], None)
            tt = t_map.get(t[i], None)
            if ss is None and tt is None:
                # no mapping yet, add and proceed
                s_map[s[i]] = t[i]
                t_map[t[i]] = s[i]
            elif s[i] == tt and t[i] == ss:
                # mapping found, proceed
                continue
            else:
                return False
        return True
```

occurrences -> Hashtable

In Python, if we need to check for `None`, use `is None`. Simply using `not` can also allow "0" and empty string to pass through. 

Also, don't overcomplicate a if clause. 





## Approach 2: Python chad solution

The `zip()` function returns a zip object, which is an iterator of tuples where the first item in each passed iterator is paired together, and then the second item in each passed iterator are paired together etc.

## Syntax

zip(*iterator1, iterator2, iterator3 ...*)

## Parameter Values

| Parameter                             | Description                                   |
| :------------------------------------ | :-------------------------------------------- |
| *iterable1, iterable2, iterable3 ...* | Iterable objects that will be joined together |

s = "egg" → pairs: [('e','a'), ('g','d'), ('g','d')] t = "add"



This proves bijec

```python
return len(set(zip(s, t))) == len(set(s)) == len(set(t))
```

